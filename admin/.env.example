# Copy to .env.local. Only the publishable key ever reaches the browser; the
# service role key belongs in the Node service's environment and nowhere else.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxx
